const Topbar = ({ role }) => {
  return (
    <header style={{ height: "60px", background: "#f3f4f6", padding: "0 20px", display: "flex", alignItems: "center" }}>
      Topbar ({role})
    </header>
  );
};

export default Topbar;

